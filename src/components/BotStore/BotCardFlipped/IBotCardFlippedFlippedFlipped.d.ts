import * as React from 'react';

export interface BotCardFlippedFlippedFlippedProps {
  className?: string;
  children?: React.ReactNode;
  Title: string;
  Description: string;
  Rating: number;
  Views: number;
  URL: string;
  Icon: string;
  Type: string;
}

export function BotCardFlippedFlippedFlipped(props: BotCardFlippedFlippedFlippedProps): React.ReactNode;
